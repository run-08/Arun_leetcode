# Write your MySQL query statement below
select Department.name as "Department" , Employee.name as "Employee" , Employee.Salary as "Salary" from Employee inner join Department on Department.id = Employee.departmentId where (Employee.salary,department.id ) in (select max(Salary) , departmentId  from Employee group by departmentId) 